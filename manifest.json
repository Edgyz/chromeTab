{
  "manifest_version": 2,

  "name": "Inès' New Tab",
  "description": "Adds a new tab with time, to do list, and stuff. WIP!",
  "version": "1.0",

  "chrome_url_overrides" : {
     "newtab": "mynewtab.html"
   },
   "background": {
     "scripts": ["background.js"]
   },
  "permissions": [
    "activeTab",
    "https://ajax.googleapis.com/"
  ]
}
